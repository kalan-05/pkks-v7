<?php
declare(strict_types=1);

/*
 * Этот шаблон не считывается сайтом и не содержит секретов.
 * Скопируйте его только во внешний закрытый runtime-каталог с правами 0600.
 * Путь передаётся через PKKS_ADMIN_RUNTIME_CONFIG и не попадает в Git.
 * Для текущего no-mail запуска задайте PKKS_ADMIN_AUTH_DELIVERY_MODE=manual.
 * SMTP-параметры добавляют только в отдельном согласованном почтовом контуре.
 */

return [
    'PKKS_ADMIN_AUTH_DB_PATH' => '/absolute/private/path/accounts.sqlite',
    'PKKS_ADMIN_BASE_URL' => 'https://example.invalid',
    'PKKS_ADMIN_AUTH_DELIVERY_MODE' => 'manual',
    'PKKS_ADMIN_MAIL_TRANSPORT' => 'disabled',
    'PKKS_ADMIN_PRIMARY_ADMIN_EMAIL' => 'primary@example.invalid',
];
